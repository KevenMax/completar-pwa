<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "Completar - Gerenciador de Horas Complementares",
  "short_name": "Completar",
  "start_url": "/s",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= image_path "logo-azul-completa.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#000000",
  "background_color": "#FFFFFF",
  "display": "fullscreen",
  "orientation": "portrait"
}
